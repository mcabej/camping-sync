{
  "name": "camping-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared trip planner for camping with friends",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
